<?xml version="1.0" ?>
<!-- 

This XML file defines the products to ingest. 
For each ProductCache a set  of information is required to manage ingestion 
and association with the metadata URN (as found in OpenWIS catalogue).

The 'ProductConfiguration' contains a list of 'ProductCache' elements.

-->
<ProductConfiguration>
    <ProductCaches>
        <ProductCache urn="TEMPLATE">
            <directory/>
            <regexp/>
            <referenceTimes>0000,0300,0600,1200,1800</referenceTimes>
            <synthesisProduct>false</synthesisProduct>
            <dateExtractor format="yyyyMMddHHmmss">
                <regexp>,(\d{14})\..*$</regexp>
            </dateExtractor>
         </ProductCache>
    </ProductCaches>
</ProductConfiguration>
